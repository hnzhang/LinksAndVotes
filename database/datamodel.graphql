type Link {
    id: ID! @unique
    description: String!
    url: String!
    postBy: ID!
}

type User {
    id: ID! @unique
    name: String!
    email: String! @unique
    password: String!
    links: [Link!]!
}