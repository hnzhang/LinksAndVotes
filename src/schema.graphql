# import Link from "./generated/schema.graphql"
type AutoPayload {
    token: String
    user: User
}
type Query{
    info: String!
    feed: [Link!]!
}

type Mutation {
    post(url: String!, description: String!) : Link!
}